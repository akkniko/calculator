# Calculator Lab with .dll operations
To use this calculator, build the project from source files and the cmake build system.

An example of how I put together the project:

```bash
cd calculator
mkdir build
cd build
cmake .. 
cmake --build .
```

To start the calculator, type the command

```bash
./mycalc.exe
```

To stop the program, press
```bash
ctrl + c
```